services:
  elysia-tiku:
    build: .
    image: elysia-tiku:latest
    container_name: elysia-tiku
    ports:
      - "300:300"
    volumes:
      # 挂载数据卷以持久化数据库和配置
      - ./data:/data
    environment:
      - PORT=300
      - DB_PATH=/data/tiku.db
    restart: unless-stopped

services:
  elysia-tiku:
    build: .
    ports:
      - "${PORT:-300}:${PORT:-300}"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/tiku.db
    volumes:
      - tiku-data:/app/data
    restart: unless-stopped

volumes:
  tiku-data:

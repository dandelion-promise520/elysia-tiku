services:
  elysia-tiku:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/tiku.db
    volumes:
      - tiku-data:/app/data
    restart: unless-stopped

volumes:
  tiku-data:
